# coding = 'utf-8"

def mean_encoder(dfs, target, encoded_vars, encoded_methods):
    """

    Parameters
    ----------
    dfs: Contains a list of cross validated set.
    target: the list of target variables to encode from
    encoded_vars: the list of encoded method
    encoded_methods: mean or something

    Returns: A list of dfs
    -------

    """